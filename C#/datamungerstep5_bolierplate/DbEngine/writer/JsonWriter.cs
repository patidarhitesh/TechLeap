namespace DbEngine.writer
{
    class JsonWriter
    {
        /*
	 This method will write the resultSet object into a JSON file. On successful writing, the method will return true, else will return false
	 */
        public bool WriteToJson()
        {
            return true;
        }
    }
}
